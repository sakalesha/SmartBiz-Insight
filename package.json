{
    "name": "smartbiz-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "vercel-build": "cd frontend && npm install && npm run build"
    },
    "workspaces": [
        "frontend",
        "backend"
    ]
}